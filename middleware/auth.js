import { cookieGet }  from '@/utils'
export default function ({store, route, req, res,  redirect}) {
    // let isClient = process.client;
    // // let isServer = process.server;
    // // let isBroswer =  process.browser
    //
    // let redirectURL = '/account?key=login'
    //
    // let token
    // let isLogin = false
    // if(isClient){
    //    token = localStorage.getItem('sid')
    //    isLogin = cookieGet('isLogin') === 'true'
    // }
    // console.dir(token)
    // if(!isLogin){
    //     store.commit('token/setIsLogin', false)
    //     redirect(redirectURL)
    // }

}